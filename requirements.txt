# Permafrost - Dependencies
# Core
requests>=2.31.0
pillow>=10.0.0

# Console (Web UI)
streamlit>=1.30.0

# AI Providers (all included so users don't hit missing module errors)
anthropic>=0.40.0
openai>=1.50.0
google-generativeai>=0.8.0
# Ollama and OpenRouter use requests, no extra deps

# LINE webhook tunnel (cloudflare = zero config, ngrok = needs account)
pycloudflared>=0.2.0

# Vector search (local embedding for semantic memory retrieval)
sentence-transformers>=2.2.0

# Document tools (optional - graceful fallback if missing)
fpdf2>=2.7.0           # PDF creation
python-docx>=1.0.0     # Word document creation
openpyxl>=3.1.0        # Excel read/write
pypdf>=4.0.0           # PDF reading
