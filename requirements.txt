# Permafrost — Dependencies
# Core
requests>=2.31.0
pillow>=10.0.0

# Console (Web UI)
streamlit>=1.30.0

# AI Providers (all included so users don't hit missing module errors)
anthropic>=0.40.0
openai>=1.50.0
google-generativeai>=0.8.0
# Ollama and OpenRouter use requests, no extra deps

# LINE webhook tunnel
pyngrok>=7.0.0
